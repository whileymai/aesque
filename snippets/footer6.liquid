{%- comment -%}
{%- endcomment -%}
<div class="tt-footer tt-footer__06{% if section.settings.footer_reverse %} tt-footer__reverse{% endif %}">
  <div class="tt-footer__content">
    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          {{ logo }}
          {% unless tape %}
            {{ copyright }}
          {% endunless %}
          {{ social }}
        </div>
      </div>
    </div>
  </div>
  {{ tape }}
  {{ btn_to_top }}
</div>